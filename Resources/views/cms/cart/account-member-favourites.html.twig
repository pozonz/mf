{% extends 'cms/cart/account-member.html.twig' %}

{% block memberContentWraper %}
    <div class="row">
        <div class="col-11 mx-auto">
            <h4 class="mb-3">Favourite Products</h4>

            <div class="row">
                {% for i in 1..4 %}
                    {% include 'includes/product.html.twig' with { col: 4, height: 320, remove: 1 } %}
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}